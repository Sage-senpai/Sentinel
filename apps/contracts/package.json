{
  "name": "@sentinel/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy-alert-registry.ts --network localhost",
    "deploy:testnet": "hardhat run scripts/deploy-alert-registry.ts --network arbitrumSepolia",
    "deploy:arbitrum": "hardhat run scripts/deploy-alert-registry.ts --network arbitrum",
    "verify": "hardhat verify --network arbitrum"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "hardhat": "^2.19.4",
    "hardhat-gas-reporter": "^1.0.10",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2",
    "@types/node": "^20.11.0"
  }
}
